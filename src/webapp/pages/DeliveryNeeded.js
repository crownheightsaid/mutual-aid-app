import React, { useState } from "react";
import Typography from "@material-ui/core/Typography";
import Box from "@material-ui/core/Box";

export default class DeliveryNeeded extends React.Component {
  render() {
    return (
      <Box>
        <Typography variant="h3">Delivery Needed</Typography>
      </Box>
    );
  }
}
